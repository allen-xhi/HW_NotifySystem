public class Student extends User{
    public Student(LanguageFamily language, String userName) {
        super(language, userName);
    }
    public void reserve(Course course,Event event) {
    }
    public void cancel(Course course,Event event) {
    }
}
